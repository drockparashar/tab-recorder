# Global ignores for the whole project

# Node.js dependencies
node_modules/

# Backend build/output
backend/recordings/
backend/.env
backend/*.log

# Extension build/output (if any in future)
exe/*.log
exe/*.env

# System files
.DS_Store
Thumbs.db

# VSCode settings
.vscode/

# OS generated files
*.swp
*.swo

# Ignore npm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore package-lock if not needed in repo
backend/package-lock.json

# Ignore test/coverage output (if any in future)
coverage/

# Ignore local scripts or temp files
*.tmp
*.bak

# Ignore compiled output (if any in future)
dist/
build/

# Ignore extension icons (SVGs are source, so keep them)
# exe/icons/ (do NOT ignore, these are needed for extension)

# Ignore environment files in root (if any)
.env
